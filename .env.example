# ============================================================
# Copia este archivo a .env y rellena tus valores reales.
# NUNCA subas .env a git (ya está en .gitignore).
# ============================================================

# --- Fuente de datos ---
# "s3"    → descarga los CSV desde AWS S3 (producción)
# "local" → usa los CSV ya montados en ./data/raw/ (pruebas sin AWS)
DATA_SOURCE=s3
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
AWS_DEFAULT_REGION=us-east-1

# Bucket donde subiste los CSVs (p1_extrac.csv … p10_extrac.csv)
S3_BUCKET_RAW=tu-bucket
S3_PREFIX_RAW=raw/

# Bucket para artefactos MLflow (puede ser el mismo u otro)
MLFLOW_ARTIFACTS_BUCKET=tu-bucket-mlflow

# --- Airflow ---
# Genera una clave con: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW_FERNET_KEY=tu_clave_fernet_aqui
